# lil-olt-metrics

## Purpose

A minimal, single-process metrics server that ingests OTLP metrics and serves them via the Prometheus HTTP API. Designed for edge deployments, dev environments, and lightweight production use.

## Functionality

- OTLP gRPC and HTTP ingestion (protobuf and JSON)
- Prometheus-compatible HTTP API for querying (works with Grafana)
- Pluggable storage: built-in FS engine, Prometheus remote, or VictoriaMetrics remote
- OTLP-to-Prometheus metric model translation (all metric types)
- Delta-to-cumulative conversion, ExponentialHistogram support, Summary passthrough
- WAL-based crash recovery with block compaction and retention
- Zero-config startup with sensible defaults

## Files

| File | Description |
|------|-------------|
| `go.mod` | Go module definition |
| `go.sum` | Go dependency checksums |
| `Makefile` | Build targets: build, test, lint, fmt, generate, vet |
| `Dockerfile` | Multi-stage distroless container build |
| `docker-compose.yml` | Local dev stack (server + Grafana) |
| `grafana-datasource.yml` | Grafana datasource provisioning for docker-compose |
| `README.md` | Project overview, quick start, and development guide |
| `CHANGELOG.md` | Release changelog |
| `.golangci.yml` | Linter configuration |
| `.gitignore` | Git ignore rules |

## Directories

| Directory | Description |
|-----------|-------------|
| `cmd/server/` | Application entrypoint |
| `internal/config/` | Configuration loading and defaults |
| `internal/ingest/` | OTLP ingestion (gRPC, HTTP, translation) |
| `internal/store/` | Storage backends (FS, MemStore, Prometheus, VictoriaMetrics) |
| `internal/query/` | Prometheus HTTP API and PromQL engine |
| `internal/integration/` | End-to-end integration tests |
| `internal/tools/` | Dev tool version pinning (gofumpt, moq, golangci-lint) |
| `docs/` | Architecture, standards, and configuration reference |
| `plans/` | Implementation roadmap and plan files |
| `.github/workflows/` | CI/CD pipeline definitions |

## TODO

| Priority | Task |
|----------|------|
| P3 | Exemplar storage and query |
| P3 | Kubernetes manifests / Helm chart |
